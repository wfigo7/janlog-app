# Janlog Backend - Local Development Environment Variables Sample
# このファイルをコピーして .env.local を作成してください
# cp .env.sample .env.local

# 環境設定
ENVIRONMENT=local

# AWS設定（ローカル開発用ダミー値）
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=dummy
AWS_SECRET_ACCESS_KEY=dummy

# DynamoDB設定（DynamoDB Local）
DYNAMODB_TABLE_NAME=janlog-table-local
DYNAMODB_ENDPOINT_URL=http://localhost:8000

# Cognito設定（ローカル開発では使用しないが設定）
COGNITO_USER_POOL_ID=local-pool
COGNITO_CLIENT_ID=local-client

# JWT設定（ローカル開発用）
JWT_SECRET_KEY=local-development-secret-key-for-testing-only
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# デバッグ設定
DEBUG=true

# 静的JWT設定（local環境用）
# 以下のコマンドで生成してください: python scripts/generate_mock_jwt.py
MOCK_JWT=<JWT_TOKEN_HERE>
JWT_ISSUER=mock-issuer
JWT_AUDIENCE=janlog-local