# Janlog Frontend - direnv設定
# このディレクトリに入ると自動で環境変数が読み込まれます

# .env.localファイルの読み込み
if [ -f ".env.local" ]; then
    dotenv .env.local
    echo "📝 .env.local loaded"
else
    echo "⚠️  .env.local not found. Please create it from .env.sample"
fi

echo "📱 Janlog Frontend environment ready!"
